// script to update color of html header tag to red
// using document.querySelector (script must be imported from the <head> tag, not at the end of the HTML)
document.addEventListener('DOMContentLoaded', function () {
  document.querySelector('header').style = 'color: red;';
});
